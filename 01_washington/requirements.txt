# Python 3.11 기준, 이 조합으로 src/01~12 전체 파이프라인 실행을 검증했다.
# (pip install -r requirements.txt 후 순서대로 실행하면 문서의 수치가 그대로 재현됨)
#
# 주의: shap은 numpy>=2를 요구하므로 matplotlib도 numpy 2를 지원하는 3.9 이상이어야 한다.
#       numpy 1.x로 내리면 shap이, matplotlib을 3.6대로 내리면 numpy 2와 충돌한다.
pandas==3.0.5
numpy==2.4.6
scikit-learn==1.9.0
scipy==1.17.1
matplotlib==3.11.1
seaborn==0.13.2
xgboost==3.2.0
shap==0.51.0
tabulate==0.10.0
