# 2026-08-19

## 요약 (여기 10줄만 봐도 파악 가능)
- **직전 작업**: "다른 사람이 같은 데이터 분석하면 최우수 모델이 다르게 나오나?" 질문에서 출발해, `log1p(cnt)` 변환 후 6개 모델을 재비교(`15_log_target_comparison.py`). **랜덤포레스트는 변환해도 1위 그대로**, XGBoost는 크게 개선(5·6위→2·3위), 선형모델은 오히려 악화(2·3·4위→4·5·6위) — 예상과 반대 결과를 그대로 `REPORT.md` 4.5절·6절·7절·8절에 반영.
- **다음 할 일**:
  1. 이번 log 변환 실험 반영분 git commit + push (`20차시` 저장소, `london_bike` 저장소 둘 다 — 현재 두 곳 다 Ridge/Lasso 추가 시점 상태까지만 반영돼 있고 이번 건은 미반영)
  2. weather_code 최빈값 동률("더 나쁜 코드 우선") 처리가 실제 몇 건 발생했는지 아직 집계 안 함 (변환 단계 이월 항목)
  3. REPORT 8절에 추가한 "무작위 분할·시간별 데이터 비교는 미검증" 항목 — 필요 시 후속 실험 가능한 상태로 남겨둠
  4. weathersit(워싱턴) "최악등급 4시간 이상" 규칙 731일 전체 재검증 안 됨 (이월 항목)
- **상태**: 워싱턴(01_washington) 완료·push 완료. 런던(02_london)은 분석+Ridge/Lasso까지는 `capital-bikeshare-portfolio`·`london_bike` 양쪽에 push 완료, **log 변환 실험(15단계)은 로컬에만 있고 아직 두 저장소 다 push 전**.

**오늘 있었던 GitHub 작업**
- `01_washington` + `02_london` 통합 상태를 `https://github.com/blanciel1977-lab/capital-bikeshare-portfolio`에 커밋·push (루트 README.md 신규 작성해 포트폴리오 랜딩페이지로 구성)
- 사용자 요청으로 `02_london`만 별도 분리해 새 공개 저장소 `https://github.com/blanciel1977-lab/london_bike` 생성(로컬 위치: `C:\Users\jungs\Documents\wiset_클로드\london_bike`, `20차시`와는 독립된 git 저장소). README의 워싱턴 링크는 원본 포트폴리오 저장소 URL로 수정
- Ridge/Lasso 추가 + 스크립트 재정렬(08~14번)을 두 저장소 모두에 반영·push. 동기화 과정에서 예전 번호의 중복 스크립트 파일이 `london_bike`에 남는 문제를 발견해 정리(향후 동기화 시 `rsync --delete` 방식 부재로 재발 가능하므로 주의)

**런던 핵심 결과 요약** (자세한 근거는 `02_london/docs/REPORT.md`)
- 최우수 모델: 랜덤포레스트 (워싱턴은 XGBoost 튜닝이 항상 1위였는데, 런던은 랜덤포레스트가 근소 우위 — 가정하지 않고 실측으로 결정)
- 습도(hum) 영향이 워싱턴보다 훨씬 강함 (상관 -0.587 vs -0.101)
- 이상치 1건(2015-07-09, 대여량 72,504건)이 런던 지하철 전면파업일과 정확히 일치함을 외부 뉴스로 검증(워싱턴의 허리케인 샌디 사례와 동일 성격)
- `yr`은 계획 단계부터 입력 제외(train/test 각각 단일값이라 학습 불가) — 워싱턴처럼 사후 보정할 필요 없이 처음부터 재사용 가능한 형태로 설계

---

## 상세 로그

### 1. 저장소 클론 및 첫 리뷰
- `capital-bikeshare-portfolio` GitHub 저장소를 빈 폴더(현재 루트)에 clone
- 처음 보는 사람 입장에서 리뷰 요청 → README/구조 요약, "결과보다 검증 과정에 초점" 특징 정리
- pandas/matplotlib/seaborn/scikit-learn/xgboost 설치 여부 확인 → 전부 이미 설치돼 있어 추가 설치 불필요

### 2. Isolation Forest 이상치 탐색 (원본 미수정, 별도 분석)
- `temp/atemp/hum/windspeed/weathersit/cnt` 6개 변수, contamination=5% → 731일 중 37일 이상치 탐지
- 상위 5일을 외부 웹 검색으로 실제 기상 기록과 대조 → 4일이 실제 사건과 정확히 일치(허리케인 샌디 2012-10-29, 2011 Snowtober, 2011년 1월 대설 등), 1일(2011-03-10, 습도=0)은 데이터 오류로 판단
- z-score 기반 원인 변수 분해: 습도(38%) > 풍속(30%) > 체감온도(24%) > 대여수(8%)
- dataviz 스킬 기준으로 원인 변수 시각화 아티팩트 제작·게시
- 결과를 `outputs/anomaly/`(anomaly_causes.html, isoforest_anomalies.csv, isoforest_root_cause.csv)에 저장
- README.md에 "이상치 탐색(Isolation Forest)" 섹션 추가

### 3. 후속 질의응답
- 가장 적게 대여된 날 = 2012-10-29 (22건, 허리케인 샌디)
- 휴일이 대여량에 미치는 영향: 전체 cnt는 유의하지 않음(p=0.08)이나, casual +75% / registered -33%로 통근↔여가 구성이 크게 바뀜을 확인

### 4. 최우수 모델(튜닝 XGBoost) 예측 오차 분석
- 731일 전체 예측 후 실제-예측 차이 상위 10일 산출 → 전부 test 구간(2012-08-07~2012-12-31)
- 이상치 37일과 대조 → 겹치는 날은 2012-10-29 단 1일뿐
- "모델 성능 부족 아닌가?" 질문에 이상치 탐지(입력이 낯선가)와 예측 오차(모델이 패턴을 설명하는가)는 다른 개념임을 근거와 함께 설명
- README.md에 "휴일 영향", "예측 오차 분석" 섹션 추가, `outputs/prediction/top10_residuals.csv` 저장
- `docs/CLAUDE_WORKFLOW.md`에 이 모든 사후 분석 과정을 섹션 9로 기록
- 커밋: `4277fde` "Add Isolation Forest anomaly exploration and prediction error analysis"

### 5. 저장소 구조 재편 (신규 데이터셋 준비)
- 사용자가 같은 폴더에 다른 데이터셋으로 신규 전처리~회귀분석을 진행하겠다고 알림
- 기존 워싱턴 프로젝트 전체(README/requirements/.gitignore/data/docs/outputs/src)를 `git mv`로 `01_washington/`으로 이동
- 신규 작업용 빈 폴더 `02_london/` 루트에 생성(.gitkeep)
- 커밋: `6a2750e` "Restructure repo for multi-dataset layout"

### 6. 시간별(hour) vs 일별(day) 원본 데이터 비교 — "원본 임의 수정 금지" 조건 하에 진행
- 사용자가 `01_washington/data/bike_sharing_hour.csv`(17,379행) 추가 제공
- 컬럼 비교: day.csv 16개 컬럼 모두 hour.csv에 포함, hour.csv는 `hr`(시간대) 컬럼만 추가된 상위 호환 구조. 기간은 둘 다 2011-01-01~2012-12-31로 동일
- 정합성 검증: 731일×24시간=17,544 기대 vs 실제 17,379행 → 165시간 결측. 날짜별 결측 집계 후 상위 5일 제시(1위 2012-10-29 23시간 결측=허리케인 샌디, 나머지는 2011년 1월 폭설 기간과 일치)
- "두 파일이 동일 대상을 측정한 데이터인가?" → hour.csv를 날짜별 합산한 cnt가 day.csv와 731일 전부 일치, temp 상관 0.9999999 → **예**로 결론

### 7. 컬럼별 집계 방식 역산 및 규칙서 작성
- day.csv 각 컬럼이 hour.csv를 어떤 방식(sum/mean/max/min/mode/first/last)으로 집계한 것인지 731일 전체에 대해 실측 대조
  - 달력형(season 등): 하루 내내 고정값
  - temp/atemp/hum/windspeed: 평균, 731/731 일치
  - casual/registered/cnt: 합계, 731/731 일치
  - weathersit: 최빈값만으로는 632/731(86.5%)만 설명됨
- 사용자가 weathersit 보정 규칙 제시("최악등급 4시간 이상이면 그 값, 아니면 최빈값") → 이번엔 **인지만 하고 데이터 미수정**
- 문서화 요청에 따라 사전 질문(결측 처리/반올림/문서 범위) 확인 후 `01_washington/docs/HOURLY_TO_DAILY_RULES.md` 작성 — 검증 안 된 부분(4시간 규칙 재검증, 동률 처리)은 "확정 필요 사항"으로 명시
- `docs/CLAUDE_WORKFLOW.md`에 6~7번 과정 추가 기록

### 8. 작업로그·CLAUDE.md 신설
- 루트에 `worklog/` 폴더 생성, 날짜 기반 파일(`YYYY-MM-DD.md`)로 관리하기로 함 — 오늘 파일 없으면 새로 생성, 있으면 갱신하는 방식
- 루트 `CLAUDE.md` 작성 (프로젝트 구조·컨벤션 요약)
- 커밋 `3a87a9c` "Add hourly-to-daily aggregation rules, root CLAUDE.md, and dated worklog" — 여기까지 반영

### 9. 런던 원본 데이터 최초 조회
- `02_london/data/raw/london_merged.csv` 로드해 행/열/자료형/결측치/기간 조회 → 17,414행×10컬럼, 결측 0건, 2015-01-04~2017-01-03(731일, 시간별)
- 컬럼: `timestamp, cnt, t1, t2, hum, wind_speed, weather_code, is_holiday, is_weekend, season` — 워싱턴과 스케일·인코딩 다름을 표로 정리해 보고
- 데이터는 조회만 하고 수정하지 않음

### 10. 워싱턴 변환 규칙서(`HOURLY_TO_DAILY_RULES.md`) 런던 적용 가능성 검토
- 컬럼을 (1)규칙 그대로 사용 가능 / (2)그대로 쓰면 안 됨(이유 포함) / (3)런던에 없어서 새로 만들어야 함 3그룹으로 표 정리
- `season`/`is_holiday`/`is_weekend`가 하루 내내 고정값인지 730일 전체 실측 확인(전부 고정값 맞음), 서머타임으로 34일이 24시간이 아님도 함께 발견
- `casual`/`registered`는 런던 원본에 아예 없어 파생 불가임을 명시

### 11. weather_code / season 정의 확정
- 웹 검색(Kaggle 공식 데이터셋 hmavrodiev/london-bike-sharing-dataset 문서)으로 weather_code 7개 코드(1,2,3,4,7,10,26)와 season(0~3) 의미를 확인
- 사용자 결정 2가지: ① weather_code는 원본 코드값을 그대로 쓰고 워싱턴 1~4 체계로 리매핑하지 않는다(다른 기간 데이터 추가 시 전처리 최소화 목적) ② 집계 규칙(연산 방식: 최악등급 4시간 이상→그 값, 아니면 최빈값)은 워싱턴과 동일하게 유지한다
- 결정 사항을 `02_london/docs/PREPROCESSING_NOTES.md`(전처리 원칙)와 `02_london/docs/CLAUDE_WORKFLOW.md`(런던 전용 워크플로우 기록, 신규 생성)에 정리

### 12. 사용자가 `02_london/data` 폴더 구조를 수동 재배치
- 변경 전: `data/raw/london_merged.csv`(원본), `data/processed/day_london.csv`(일별 변환본) + `data/processed/day_london_2015.csv`, `day_london_2016.csv`(연도별 분리본)
- 변경 후(실제 확인): `data/preprocess/london_merged.csv`, `data/preprocess/day_london.csv` / `data/processed/day_london_2015.csv`, `day_london_2016.csv`만 남음
- git에는 아직 하나도 커밋된 적 없어 `git mv` 이력 추적 불가 — `find`로 실제 파일 위치만 확인
- 이 변경으로 `02_london/docs/*.md`와 `02_london/src/01_hourly_to_daily.py`에 적힌 경로가 실제와 어긋남 → 사용자 확인 후 경로 일괄 정정 완료(`data/preprocess/`=원본+일별변환본, `data/processed/`=연도별 분리본), `02_london/src/02_split_by_year.py` 신규 작성해 1회성 코드를 재현 가능한 스크립트로 정리

### 13. 통합 분석 계획 수립(`ANALYSIS_PLAN.md`)
- 사용자가 `01_washington/` 전체(전처리~워크플로우)를 참고해 런던용 통합 계획 수립 요청 — "2015 학습 → 2016 예측"이 목표임을 명시
- 워싱턴 REPORT.md를 재검토해 방법론 재확인, train(2015)/test(2016) 실측 통계로 `yr` 제외(단일값이라 학습 불가)·`t1` 제외(t2와 상관 0.992)·`is_weekend` 제외(weekday 완전 파생) 결정
- `02_london/docs/ANALYSIS_PLAN.md`로 저장 (최종 입력 9개 변수, 12단계 스크립트 구성안)

### 14. 계획 실행 — 03~12 스크립트 전체 실행, REPORT.md 작성
- 실행 전 미결 3항목(season~mnth 다중공선성 처리, 튜닝 그리드 범위, contamination 비율) 질문 → 전부 "워싱턴과 동일" 채택 확인 후 착수
- `common.py`, `03_data_quality_check.py` ~ `12_anomaly_detection.py` 총 11개 스크립트 신규 작성·순차 실행
- 사소한 버그 2건 발견 즉시 수정: EDA 스크립트 한글 폰트 미설정(Malgun Gothic 지정으로 해결), weather_code boxplot의 float형-order 리스트 길이 불일치(int 캐스팅으로 해결)
- **핵심 결과**: 랜덤포레스트가 최우수 모델(RMSE 4,308.82, R2 0.7556, baseline 대비 50.8% 개선) — 워싱턴은 항상 XGBoost 튜닝이 1위였는데 이번엔 다름. `t2`+`hum` 변수중요도 합산 65.4%. 이상치 37건(5.09%) 중 2015-07-09(대여량 72,504건)가 런던 지하철 파업일과 정확히 일치함을 외부 뉴스 검색으로 검증(워싱턴 허리케인 샌디 사례와 동일 성격)
- `02_london/docs/REPORT.md`를 워싱턴과 동일한 8절 구조로 작성, 워싱턴과 달랐던 부분(최우수 모델, 습도 영향력)을 숨기지 않고 명시

## 미커밋 변경 사항 (다음 세션 시작 시 git status 로 확인)
- `02_london/` 전체 (data, docs, src, outputs) — 신규/수정 다수, untracked
- `worklog/2026-08-19.md` (수정, 이번 갱신분)
- `worklog/2026-08-19.md` (신규)
- `CLAUDE.md` (신규)
