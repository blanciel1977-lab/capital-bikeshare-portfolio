# 프로젝트 안내

여러 자전거 대여 데이터셋을 각각 독립적으로 분석하는 루트 폴더. 각 데이터셋은 번호가 붙은 하위 폴더로 분리한다.

## 폴더 구조

```
.
├── 01_washington/    Capital Bikeshare(워싱턴 D.C.) 분석 완료 — README.md 및 01_washington/docs/ 참고
├── 02_london/        런던 데이터셋 (착수 전, 폴더만 존재)
├── worklog/          날짜별 작업로그 (YYYY-MM-DD.md)
└── CLAUDE.md         이 파일
```

## 작업 원칙

- **원본 데이터는 임의로 수정하지 않는다.** 조회·분석은 자유롭게 하되, `data/` 아래 원본 CSV를 고치거나 덮어쓰지 않는다.
- 데이터셋별 산출물은 해당 번호 폴더 안에서만 다룬다 (예: 워싱턴 결과는 `01_washington/outputs/`에, 런던 결과는 `02_london/outputs/`에).
- 시간별→일별 변환이 필요하면 `01_washington/docs/HOURLY_TO_DAILY_RULES.md`의 집계 규칙을 먼저 참고하고, 새 데이터셋에 적용하기 전 반드시 재검증한다.

## 작업로그

- 세션마다 있었던 일을 `worklog/YYYY-MM-DD.md`에 기록한다.
- **오늘 날짜 파일이 없으면 새로 만들고, 있으면 그 파일을 이어서 업데이트한다.**
- 각 파일 맨 위에 "요약" 블록(직전 작업 / 다음 할 일 / 상태)을 두어, 상단 10줄만 읽어도 이어서 작업할 수 있게 한다.

## 세부 워크플로우 기록

각 데이터셋 폴더 안의 `docs/CLAUDE_WORKFLOW.md`에 그 데이터셋에 대한 분석 과정(가설-검증-보정 사이클)을 단계별로 상세히 남긴다. `worklog/`가 "무엇을 언제 했는지"의 타임라인이라면, `CLAUDE_WORKFLOW.md`는 "왜 그렇게 했는지"의 서사에 가깝다.
